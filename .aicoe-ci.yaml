check:
  - thoth-build
build:
  build-stratergy: Dockerfile
  dockerfile-path: Dockerfile
  registry: quay.io
  registry-org: s2i-python-anaconda-base
  registry-project: package-extract
  registry-secret: modh-pusher-secret
  custom-tag: latest
